server:
  port: 8761
eureka:
  client:
    # 是否注册自己
    register-with-eureka: false
    # 是否从其他地方导入数据
    fetch-registry: false
    service-url:
      # 注册中心位置
      defaultZone: http://localhost:8761/eureka/