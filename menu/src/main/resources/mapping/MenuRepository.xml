<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.southwind.repository.MenuRepository">

	<!--<resultMap id="menuMap" type="Menu">-->
		<!--<id property="id" column="mid"/>-->
		<!--<result property="name" column="mname"/>-->
		<!--<result property="author" column="author"/>-->
		<!--<result property="price" column="price"/>-->
		<!--<result property="flavor" column="flavor"/>-->
		<!--&lt;!&ndash; 映射type &ndash;&gt;-->
		<!--<association property="type" javaType="Type">-->
			<!--<id property="id" column="tid"/>-->
			<!--<result property="name" column="tname"/>-->
		<!--</association>-->
	<!--</resultMap>-->

    <select id="findAll" resultType="com.southwind.entity.Menu">
		select * from t_menu limit #{param1},#{param2};
	</select>

	<select id="count" resultType="int">
		select count(id) from t_menu;
	</select>

	<insert id="save" parameterType="com.southwind.entity.Menu">
		insert into t_menu(name,price,flavor) values(#{name},#{price},#{flavor})
	</insert>

	<select id="findById" parameterType="long" resultType="com.southwind.entity.Menu">
		select * from t_menu where id = #{id}
	</select>

	<update id="update" parameterType="com.southwind.entity.Menu">
		update t_menu set name = #{name},price = #{price},flavor = #{flavor} where id = #{id}
	</update>

	<delete id="deleteById" parameterType="long">
		delete from t_menu where id = #{id}
	</delete>
</mapper>